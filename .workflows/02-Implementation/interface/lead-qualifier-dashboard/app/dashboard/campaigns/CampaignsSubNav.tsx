"use client";

/**
 * Navigation campagnes (Overview / Suivi détaillé) : uniquement dans la sidebar.
 * Fichier conservé pour casser d’éventuelles dépendances / cache de build sur l’ancien chemin.
 */
export function CampaignsSubNav() {
  return null;
}
